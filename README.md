# Система управления производством (Production Management System)

Веб-приложение для автоматизации и управления производственными процессами. Система позволяет контролировать запасы сырья, планировать выпуск продукции, отслеживать загрузку производственных линий и управлять статусами заказов.

![Страница главная](screenshot.png)

## 🛠 Технологии

Проект разработан на современном стеке Microsoft:

*   **Backend:** ASP.NET Core 9.0 (MVC)
*   **Язык:** C# (NET 9.0)
*   **База данных:** SQLite (через Entity Framework Core)
*   **Frontend:** HTML5, CSS3, JavaScript (ES6+)
*   **UI Framework:** Bootstrap 5
*   **ORM:** Entity Framework Core

## ✨ Основные возможности

### 📦 Управление материалами (Склад)
*   Создание и редактирование справочника материалов.
*   Учет количества на складе и единиц измерения.
*   Настройка **минимального запаса** (система подсвечивает дефицит).
*   Функция быстрого пополнения склада через модальное окно.
*   Защита от дубликатов и отрицательных значений.

###  Управление продуктами
*   Создание карточек продукции.
*   Указание нормативного времени производства на единицу.
*   **Рецепты (BOM):** Привязка необходимых материалов к каждому продукту с указанием расхода на единицу.
*   Категоризация продукции и поиск.

### ⚙️ Производственные линии
*   Управление станками/линиями (название, коэффициент эффективности).
*   Смена статусов: `Активна` / `Остановлена`.
*   Автоматический расчёт времени выполнения заказа на основе эффективности линии.

### 📋 Управление заказами (Work Orders)
*   Создание производственных заказов с выбором продукта и линии.
*   Автоматический расчёт даты окончания (`EstimatedEndDate`) при создании.
*   Проверка наличия материалов перед запуском заказа.
*   Назначение заказов на свободные линии.
*   Отслеживание статусов: `Ожидает`, `В процессе`, `Завершён`, `Отменён`.

## 🚀 Установка и запуск

### Предварительные требования
Для работы приложения необходимо наличие:
*   [.NET SDK 9.0](https://dotnet.microsoft.com/download/dotnet/9.0) или новее.

### Запуск проекта

1.  Откройте терминал в папке проекта:
    ```bash
    cd ProductionManagementSystem.Web
    ```

2.  Восстановите зависимости:
    ```bash
    dotnet restore
    ```

3.  Создайте и примените миграции базы данных:
    ```bash
    dotnet ef database update
    ```
    *(Если миграции отсутствуют, создайте их командой `dotnet ef migrations add InitialCreate`)*

4.  Запустите приложение:
    ```bash
    dotnet run
    ```

5.  Откройте браузер и перейдите по адресу: [http://localhost:5028](http://localhost:5028) (или другой порт, указанный в терминале).

## 🏗 Архитектура проекта

### Структура ключевых компонентов:

*   **Models:** Содержит сущности БД (`Material`, `Product`, `ProductionLine`, `WorkOrder`) и промежуточные классы (`ProductMaterial` для связи "многие ко многим").
*   **Data:** Контекст базы данных `ApplicationDbContext` и конфигурация связей (Fluent API).
*   **Controllers:** Обработка логики запросов, валидация данных и взаимодействие с БД.
*   **Views:** Razor-представления с адаптивной версткой.

### Особенности реализации:
*   Используется подход **Code First** для базы данных.
*   Валидация данных осуществляется как на уровне моделей (`DataAnnotations`), так и на уровне контроллеров (проверка уникальности имен, наличие материалов).
*   Для сложных форм создания продуктов используется паттерн **ViewModel** (`ProductFormViewModel`), позволяющий передавать в представление дополнительные списки для выбора материалов.